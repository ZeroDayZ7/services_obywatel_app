name: Staging - Gateway GitOps

on:
  push:
    branches: [staging]
    paths:
      - "platform/services/gateway/**"

permissions:
  contents: write
  packages: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Prepare variables
        id: vars
        run: |
          echo "sha_short=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT
          echo "owner_lc=${GITHUB_REPOSITORY_OWNER,,}" >> $GITHUB_OUTPUT

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & Push Gateway
        uses: docker/build-push-action@v5
        with:
          context: ./platform/services/gateway
          file: ./platform/services/gateway/Dockerfile
          push: true
          tags: |
            ghcr.io/${{ steps.vars.outputs.owner_lc }}/obywatel-gateway:${{ steps.vars.outputs.sha_short }}
            ghcr.io/${{ steps.vars.outputs.owner_lc }}/obywatel-gateway:staging
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Update Infra Repo (GitOps)
        env:
          INFRA_TOKEN: ${{ secrets.INFRA_REPO_TOKEN }}
          # OWNER: ${{ vars.GHCR_OWNER }}
          OWNER: ${{ steps.vars.outputs.owner_lc }}
          # IMAGE_NAME: ${{ vars.GHCR_IMAGE_GATEWAY }}
          IMAGE_NAME: obywatel-gateway
          SHA: ${{ steps.vars.outputs.sha_short }}
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "github-actions@github.com"

          # Klonujemy repo infra
          git clone https://$INFRA_TOKEN@github.com/${{ github.repository_owner }}/k3s-obywatel-plus-gitops.git temp_infra
          cd temp_infra

          # Podmieniamy obraz używając dynamicznych zmiennych $IMAGE_NAME i $OWNER
          sed -i "s|image: ghcr.io/.*/$IMAGE_NAME:.*|image: ghcr.io/$OWNER/$IMAGE_NAME:$SHA|g" base/services/gateway/deployment.yaml

          git add .
          git commit -m "feat(gateway): deploy version $SHA" || echo "No changes"
          git push origin main
